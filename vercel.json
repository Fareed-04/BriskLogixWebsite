{
  "version": 2,
  "buildCommand": "NODE_OPTIONS=--max_old_space_size=4096 npm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
} 